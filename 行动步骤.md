# 你说得对，我收敛成一个**可执行方案**

下面不给你“很多建议”，只给你一个**按顺序做**的学习路线。  
目标只有一个：

- **你自己手敲一个最小版 deep research backend**
- **重点学 LangGraph 和 agent 搭建**
- **不重写前端**

---

# 总原则

你**不要直接重敲这个项目**。  
你要做的是：

- 新建一个 `backend_langgraph_learning`
- 用 **5 个版本** 逐步实现
- 每个版本都要“能跑”
- 每个版本只学一个重点

---

# 你现在就照这个顺序做

## 第 0 步：准备一个独立学习目录
不要在原项目里直接改。

### 你要建的文件
```text
backend_langgraph_learning/
  src/
    main.py
    state.py
    graph.py
    nodes.py
    prompts.py
    tools.py
```

### 这一阶段不要做
- 不做前端
- 不做 notes
- 不做多线程
- 不做 streaming
- 不做复杂配置

### 完成标准
你能启动一个 FastAPI 空服务。

---

## 第 1 步：手敲一个“单节点 LangGraph”
先别做 research loop，只做最简单版本。

### 你要实现的功能
用户传一个 `topic`，图里只跑一个节点：

```text
START -> generate_report -> END
```

### 你要写的 state
`state.py`
```python
class ResearchState(TypedDict):
    topic: str
    final_report: str
```

### 你要写的 node
`nodes.py`
```python
def generate_report(state: ResearchState) -> dict:
    ...
```

### 你要写的 graph
`graph.py`
- 建一个 `StateGraph`
- 只有一个节点 `generate_report`

### 你要写的接口
[main.py](cci:7://file:///d:/Desktop/AI-Better/Project/Agent-%E8%B5%9B%E5%8D%9A%E5%B0%8F%E9%95%87/backend/langgraph/main.py:0:0-0:0)
- `POST /research`
- 输入 `topic`
- 返回 `final_report`

### 这一阶段你在学什么
- LangGraph 最基本的：
  - state 是什么
  - node 返回什么
  - graph 怎么连

### 完成标准
你能请求：

```json
{"topic": "什么是 LangGraph"}
```

然后拿到一段报告。

---

## 第 2 步：把“单报告”改成“先规划任务”
这一版只加一个 planner。

### 目标流程
```text
START -> plan_tasks -> report_plan -> END
```

### 你把 state 改成
```python
class ResearchState(TypedDict):
    topic: str
    todo_items: list[dict]
    final_report: str
```

### 新增 node
- `plan_tasks`
- `report_plan`

### `plan_tasks` 要做的事
让 LLM 把 topic 拆成 3 个任务，输出结构化结果，例如：

```python
[
    {"title": "...", "query": "..."},
    {"title": "...", "query": "..."},
    {"title": "...", "query": "..."},
]
```

### 这一阶段不要做
- 不做真实 web search
- 不做 task loop
- 不做并发

### 这一阶段你在学什么
- 为什么 agent 先规划再执行
- state 里怎么放结构化中间结果

### 完成标准
接口返回的不是一段瞎写的大报告，而是：
- 3 个任务
- 一段简单的计划总结

---

## 第 3 步：加入“任务循环”
这是你真正该学的 LangGraph 核心。

### 目标流程
```text
START
-> plan_tasks
-> pick_task
-> run_task
-> should_continue
   -> pick_task
   -> final_report
-> END
```

### 你把 state 改成
```python
class ResearchState(TypedDict):
    topic: str
    todo_items: list[dict]
    current_task_index: int
    task_results: list[dict]
    final_report: str
```

### 你新增的 node
- `pick_task`
- `run_task`
- `should_continue`
- `final_report`

### `run_task` 先不要真的搜索
你先写死一个假搜索：

```python
def search_web(query: str) -> list[dict]:
    return [{"title": "mock result", "snippet": "mock snippet"}]
```

### 这一阶段你在学什么
- LangGraph 为什么适合流程循环
- 条件边怎么写
- state 怎么在循环中更新

### 完成标准
输入一个 topic 后：
- 先生成 3 个任务
- 再逐个执行
- 最后生成汇总报告

---

## 第 4 步：把假搜索换成真实搜索工具
现在才开始接工具。

### 你要做的事
把 `tools.py` 里的 `search_web` 换成真实实现。

### 建议只接一个搜索源
只选一个，别贪多：

- **首选**：DuckDuckGo
- 或你当前项目里已经能用的搜索方式

### 这一阶段不要做
- 不做多搜索源兼容
- 不做 provider 切换
- 不做复杂 fallback

### 这一阶段你在学什么
- tool 和 graph node 的关系
- 为什么工具结果不应该直接原样塞给最终报告
- 为什么每个 task 都应该先“查”，再“总结”

### 完成标准
每个 task 都能拿到真实 search 结果，再写入 `task_results`。

---

## 第 5 步：加入 streaming
这一步才开始接近你现在这个项目。

### 目标
让后端不是“最后一次性返回”，而是边执行边吐事件。

### 你要做的事
新增一个流式接口：

```text
POST /research/stream
```

输出事件类型先只保留这 4 种：

- `todo_list`
- `task_started`
- `task_finished`
- `final_report`

### 这一阶段不要做
- 不做多线程
- 不做并行 task
- 不做复杂事件模型

### 这一阶段你在学什么
- agent 为什么需要“过程可见”
- 后端执行过程如何映射成前端事件流

### 完成标准
前端或 Postman 能看到任务一步一步执行，不是最后才出结果。

---

# 你怎么利用现在这个项目

你不是重敲它。  
你是把它当“参考答案库”，按下面顺序看。

## 只看这几个点

### 第一步看
[main.py](cci:7://file:///d:/Desktop/AI-Better/Project/Agent-%E8%B5%9B%E5%8D%9A%E5%B0%8F%E9%95%87/backend/langgraph/main.py:0:0-0:0)

你只看：
- `/research`
- `/research/stream`

你要理解的是：
- 接口层长什么样
- 输入输出模型怎么定义

---

### 第二步看
[agent.py](cci:7://file:///d:/Desktop/AI-Better/study/helloagents-deepresearch/backend/src/agent.py:0:0-0:0)

你只看：
- [run](cci:1://file:///d:/Desktop/AI-Better/study/helloagents-deepresearch/backend/src/agent.py:124:4-147:9)
- [run_stream](cci:1://file:///d:/Desktop/AI-Better/study/helloagents-deepresearch/backend/src/agent.py:149:4-283:30)

你要理解的是：
- 整个 agent 的执行主线是什么
- 哪些阶段是 planner / task / report

---

### 第三步看
`services` 相关文件
你只看：
- planner
- summarizer
- reporting

你要理解的是：
- 为什么拆成这三个职责

---

# 你这周只需要做这 3 件事

## Day 1
做完 **第 1 步**

目标：
- 单节点 LangGraph
- 一个 `/research`
- 能返回报告

---

## Day 2
做完 **第 2 步**

目标：
- topic -> todo list
- 不再是一次性大报告

---

## Day 3-4
做完 **第 3 步**

目标：
- 任务循环
- 条件边
- 汇总报告

这一步最重要。

---

## Day 5
做完 **第 4 步**

目标：
- 接真实搜索工具

---

## Day 6
做完 **第 5 步**

目标：
- 流式输出

---

# 你每一步都必须自检

每完成一版，只问自己这 3 个问题：

- **这个 state 字段为什么存在？**
- **这个 node 为什么独立？**
- **这个流程为什么要 graph，而不是普通函数？**

如果答不上来，就不要进下一步。

---

# 你现在最该做的事

**不要继续看大项目。**

你现在直接开始做：

## 立刻执行
- 新建 `backend_langgraph_learning`
- 实现 **第 1 步：单节点 LangGraph**
- 只写：
  - `state.py`
  - `nodes.py`
  - `graph.py`
  - [main.py](cci:7://file:///d:/Desktop/AI-Better/Project/Agent-%E8%B5%9B%E5%8D%9A%E5%B0%8F%E9%95%87/backend/langgraph/main.py:0:0-0:0)

---

# 如果你想让我继续帮你

我下一条可以直接给你：

## 选项 A
**第 1 步的最小文件清单 + 每个文件该写什么**

## 选项 B
**第 1 步到第 3 步的状态设计模板**

如果你要最直接的，我建议你回我一句：

```text
给我第1步的文件清单
```

# 当前状态

- **已收敛**成 5 步可执行学习路线
- **已明确**每一步做什么、不做什么、完成标准是什么
- **建议你现在只做第 1 步**