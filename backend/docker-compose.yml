version: "3.8"

services:
  backend:
    build: .
    container_name: deep-research-agent
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # 持久化 Chroma 数据，容器删了数据还在
      - ./data:/app/data
